SF_ACCOUNT:
    constraints:
        unique:
            - account_id
        not_null:
            - account_id

SF_CONTACT:
    constraints:
        unique:
            - contact_id
        not_null:
            - contact_id

SF_LEAD:
    constraints:
        unique:
            - lead_id
        not_null:
            - lead_id

SF_OPPORTUNITY_HISTORY:
    constraints:
        unique:
            - opportunity_history_id
        not_null:
            - opportunity_history_id

SF_OPPORTUNITY:
    constraints:
        unique:
            - opportunity_id
        not_null:
            - opportunity_id

SF_USER:
    constraints:
        unique:
            - user_id
        not_null:
            - user_id
